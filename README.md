# RepoSentry
RepoSentry is a fast and lightweight CLI tool that analyzes local and GitHub repositories for common security risks and project quality issues. It scans codebases to detect patterns like hardcoded secrets, unsafe functions, debug configurations, and weak practices, while also highlighting missing project essentials.
